Mission Control static assets live here when future phases move inline CSS or icons into versioned files.
MC-001 keeps styling in `dashboard.mission_control.theme` to avoid adding a separate asset pipeline.
