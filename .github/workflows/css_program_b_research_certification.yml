name: CSS Program B Research Certification

on:
  push:
    branches:
      - program-b-research-certification-2026-09-15
    paths:
      - "backend/research/**"
      - "tests/test_program_b_research_certification.py"
      - "docs/governance/PROGRAM_B_RESEARCH_CERTIFICATION_FOUNDATION.md"
      - ".github/workflows/css_program_b_research_certification.yml"
  workflow_dispatch:

jobs:
  program-b-research-certification:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          submodules: false
      - name: Set Up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt
          python -m pip install pytest
      - name: Compile
        run: |
          python -m compileall -q backend/research tests/test_program_b_research_certification.py
      - name: Focused Program B Tests
        run: |
          python -m pytest -q tests/test_program_b_research_certification.py
      - name: Full Regression
        run: |
          python -m pytest -q
