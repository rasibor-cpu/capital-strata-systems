"""Phase 177C — broker plugins package (capability advertisement only)."""

from __future__ import annotations

# Plugins are declared in canonical_tier1._PLUGIN_BUILDERS.
# This package exists so future brokers can add modules without touching the execution engine.

__all__: list[str] = []
